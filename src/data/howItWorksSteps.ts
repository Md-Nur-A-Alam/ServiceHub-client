export interface Step {
  number: number;
  title: string;
  desc: string;
}

export const steps: Step[] = [
  { number: 1, title: "Find Service", desc: "Browse categories to find local providers." },
];
